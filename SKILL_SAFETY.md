# Skill 使用机制与安全性说明

## 🔍 核心机制

### 调用方式：只读操作
当在 VSCode Copilot 中使用 `$skill-name` 时：

```
用户输入: 请用 $agent-check 检查 plan.json
    ↓
Copilot 读取: ~/.codex/skills/agent-check/SKILL.md（只读）
    ↓
Copilot 理解: SKILL.md 中的指令和工作流程
    ↓
Copilot 执行: 按照 SKILL.md 的定义操作
    ↓
操作目标: 你的项目文件（不是 skill 文件）
```

**重点**：
- ✅ **只读取** skill 文件，不修改
- ✅ 执行操作作用于**项目文件**，不是 skill 本身
- ✅ Skill 文件保持不变

---

## 🛡️ 安全性分析

### 1. 文件权限验证

```bash
$ ls -la ~/.codex/skills/agent-check/SKILL.md
-rw-r--r--  1 haitian  staff  3324  2月13日 18:58 SKILL.md
```

权限说明：
- `-rw-r--r--` (644)
- 所有者：读写
- 组/其他：只读
- **多个进程可以同时读取，不会互相影响**

### 2. 并发访问测试

```bash
# 模拟多个程序同时读取
$ for i in {1..5}; do cat ~/.codex/skills/agent-check/SKILL.md > /dev/null & done
$ wait
# ✅ 成功，无冲突
```

### 3. 资源隔离

| 组件 | 位置 | 作用域 | 修改权限 |
|------|------|--------|----------|
| Skill 文件 | `~/.codex/skills/` | 全局共享 | 只读使用 |
| 项目配置 | `项目/AGENTS.md` | 项目级 | 项目可写 |
| 执行结果 | `项目/runs/` 等 | 项目级 | 项目可写 |
| Copilot 缓存 | VSCode 内存 | 进程级 | 临时存储 |

---

## ✅ 不会影响其他程序

### 原因 1: 只读操作
```python
# 系统调用类似于：
with open('~/.codex/skills/agent-check/SKILL.md', 'r') as f:
    content = f.read()  # 只读，不写入
    # 使用 content 进行后续操作
```

### 原因 2: 无文件锁
Linux/macOS 对普通读操作不加锁，多个进程可以同时读取：
```bash
# 进程 1 (VSCode A)
cat ~/.codex/skills/agent-check/SKILL.md

# 进程 2 (VSCode B) - 同时执行
cat ~/.codex/skills/agent-check/SKILL.md

# 进程 3 (其他程序) - 同时执行
python script.py  # 也读取同一个 SKILL.md

# ✅ 三者互不影响
```

### 原因 3: 项目隔离
每个项目通过自己的 `AGENTS.md` 声明使用哪些 skills：

```
项目 A (rootrule)              项目 B (其他项目)
├── AGENTS.md                  ├── AGENTS.md
│   └── 声明使用 agent-check     │   └── 声明使用 code-review
├── plan.json                  ├── review.json
└── runs/                      └── reports/
    └── 结果输出                    └── 结果输出
         ↓                              ↓
    都读取 ~/.codex/skills/ 中的 skills（只读）
         ↓                              ↓
    在各自项目目录中操作（互不干扰）
```

---

## 🔄 实际使用场景

### 场景 1: 多个 VSCode 窗口

```
VSCode 窗口 1 (项目 A)          VSCode 窗口 2 (项目 B)
请用 $agent-check 检查          请用 $agent-check 检查
    ↓                              ↓
读取 SKILL.md ←———————————————→ 读取 SKILL.md
    ↓                              ↓
操作 项目A/plan.json            操作 项目B/plan.json
    ↓                              ↓
输出到 项目A/runs/              输出到 项目B/runs/

✅ 完全独立，互不影响
```

### 场景 2: 命令行脚本同时运行

```bash
# 终端 1
cd /project-a
python3 ~/.codex/skills/agent-check/scripts/orchestrate_plan.py plan.json

# 终端 2（同时）
cd /project-b  
python3 ~/.codex/skills/agent-check/scripts/orchestrate_plan.py plan.json

# ✅ 两者都读取同样的 skill 脚本
# ✅ 但操作各自项目的文件
# ✅ 互不干扰
```

### 场景 3: CI/CD 并行构建

```yaml
# GitHub Actions 并行任务
jobs:
  test-project-a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run agent-check
        run: |
          # 使用共享的 skill
          $agent-check check plan.json
  
  test-project-b:  # 同时运行
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run agent-check
        run: |
          # 使用同一个 skill，不冲突
          $agent-check check plan.json
```

---

## ⚠️ 唯一需要注意的情况

### 什么时候会有影响？

只有当你**修改 skill 文件本身**时才可能影响其他使用：

```bash
# ❌ 这会影响所有使用该 skill 的项目
vim ~/.codex/skills/agent-check/SKILL.md  # 编辑中
```

**解决方案**：

1. **版本化 skill**
```bash
~/.codex/skills/
├── agent-check/          # 稳定版本
├── agent-check-v2/       # 新版本测试
└── agent-check-dev/      # 开发版本
```

2. **项目本地 skill**（高级用法）
```bash
my-project/
├── .skills/              # 项目专用 skills
│   └── custom-check/
│       └── SKILL.md
└── AGENTS.md            # 指向本地 skill
```

3. **Git 版本控制 skills**
```bash
cd ~/.codex/skills/agent-check
git init
git add .
git commit -m "v1.0.0"
git tag v1.0.0

# 其他项目可以指定版本
cd ~/.codex/skills/
git clone ... agent-check-v1
git clone ... agent-check-v2
```

---

## 📊 对比表

| 操作 | 是否影响其他程序 | 原因 |
|------|-----------------|------|
| 使用 `$skill-name` | ❌ 不影响 | 只读操作 |
| 多个项目同时使用同一 skill | ❌ 不影响 | 并发读取安全 |
| 在项目中执行 skill 命令 | ❌ 不影响 | 操作目标是项目文件 |
| VSCode 重启 | ❌ 不影响 | Skill 是持久化文件 |
| 修改 SKILL.md | ⚠️ 会影响 | 改变了 skill 定义 |
| 修改 AGENTS.md | ❌ 不影响 | 只影响当前项目 |
| 删除 skill | ✅ 会影响 | 所有依赖该 skill 的项目失效 |

---

## 🎯 最佳实践

### 1. Skill 稳定性
```bash
# 测试新版本前，先备份
cp -r ~/.codex/skills/agent-check \
      ~/.codex/skills/agent-check-backup

# 或使用 Git 版本控制
cd ~/.codex/skills/agent-check
git tag v1.0.0-stable
```

### 2. 项目依赖声明
在 `AGENTS.md` 中记录 skill 版本：

```markdown
### agent-check
**路径**: `~/.codex/skills/agent-check/SKILL.md`
**版本**: v1.0.0
**最后验证**: 2026-02-13
```

### 3. 团队共享
```bash
# 团队成员安装相同的 skills
cd ~/.codex/skills
git clone https://github.com/team/agent-check.git
git checkout v1.0.0  # 使用相同版本
```

### 4. 隔离测试
```bash
# 测试新 skill 时，使用不同名称
mkdir ~/.codex/skills/agent-check-test
# 开发完成后再替换正式版本
```

---

## 📝 总结

### ✅ 安全使用
1. **正常使用 `$skill-name`**：完全安全，只读操作
2. **多项目并发**：完全支持，互不干扰
3. **多人使用**：每个用户有自己的 `~/.codex/skills/`

### ⚠️ 需要注意
1. **修改 skill 文件**：会影响所有使用该 skill 的项目
2. **删除 skill**：会导致依赖项目失败
3. **版本不一致**：团队成员应使用相同版本

### 🚀 推荐做法
- 稳定的 skills 使用 Git 版本控制
- 重要更新前先备份或创建新版本
- 在项目 AGENTS.md 中记录使用的 skill 版本
- 测试环境使用独立的 skill 副本

---

**结论**：在 VSCode Copilot 中使用 `$skill-name` **不会影响其他程序**，因为这是只读操作，多个程序可以安全地并发读取同一个 skill 文件。
